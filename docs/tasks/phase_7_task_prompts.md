# AIM Phase 7 Task Prompts

Phase 7: Deferred Student Web App

Important:
This Phase 7 is intentionally deferred.
It must be executed only after all Phase 1..18 final reviews are complete.

Repository:
https://github.com/YousefAlgharasi/aim-platform

## Global Phase 7 Rules

- This is a late/deferred Phase 7.
- Do not start Phase 7 before completing all Phase 1..18 final reviews.
- Work on one task only.
- Claim the task before editing files.
- Use the exact branch from the task.
- All Student Web App UI must follow docs/design/source/aim-design-system.
- Student Web App must consume backend APIs only.
- Student Web App must not call the database directly.
- Student Web App must not call AIM Engine directly.
- Student Web App must not calculate or mutate official learning, placement, assessment, billing, notification, analytics, or AIM authority.
- No secrets may be committed.

---
#P7-001

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-001 only.

Task:
Create Deferred Phase 7 Student Web App Charter

Branch:
phase7/P7-001-deferred-student-web-app-charter

Priority:
P0

Description:
Define Phase 7 as a deferred post-Phase-18 Student Web App phase.

Goal:
Complete Create Deferred Phase 7 Student Web App Charter safely for the deferred Student Web App phase.

Expected output:
docs/phase-7/student-web-app-charter.md

Dependencies:
P18-091

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-001:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-002

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-002 only.

Task:
Create Phase 7 Execution Order Warning

Branch:
phase7/P7-002-execution-order-warning

Priority:
P0

Description:
Document that Phase 7 must run after all Phase 1..18 final reviews.

Goal:
Complete Create Phase 7 Execution Order Warning safely for the deferred Student Web App phase.

Expected output:
docs/phase-7/execution-order-warning.md

Dependencies:
P7-001

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-002:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-003

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-003 only.

Task:
Create Student Web Domain Map

Branch:
phase7/P7-003-student-web-domain-map

Priority:
P0

Description:
Map student web feature boundaries, flows, entities, and backend dependencies.

Goal:
Complete Create Student Web Domain Map safely for the deferred Student Web App phase.

Expected output:
docs/phase-7/student-web-domain-map.md

Dependencies:
P7-001

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-003:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-004

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-004 only.

Task:
Create Student Web Authority Rules

Branch:
phase7/P7-004-student-web-authority-rules

Priority:
P0

Description:
Define browser-client authority limits and backend/AIM authority boundaries.

Goal:
Complete Create Student Web Authority Rules safely for the deferred Student Web App phase.

Expected output:
docs/phase-7/student-web-authority-rules.md

Dependencies:
P7-001

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-004:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-005

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-005 only.

Task:
Create Student Web Design System Rules

Branch:
phase7/P7-005-student-web-design-system-rules

Priority:
P0

Description:
Document how all Student Web UI must use the approved AIM design system.

Goal:
Complete Create Student Web Design System Rules safely for the deferred Student Web App phase.

Expected output:
docs/phase-7/student-web-design-system-rules.md

Dependencies:
P7-001, DES-001

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-005:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-006

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-006 only.

Task:
Create Student Web Route Map

Branch:
phase7/P7-006-student-web-route-map

Priority:
P0

Description:
Map all Student Web routes for auth, dashboard, learning, assessments, AI, notifications, billing, reports, and support.

Goal:
Complete Create Student Web Route Map safely for the deferred Student Web App phase.

Expected output:
docs/phase-7/student-web-route-map.md

Dependencies:
P7-003

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-006:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-007

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-007 only.

Task:
Create Student Web API Contract Map

Branch:
phase7/P7-007-student-web-api-contract-map

Priority:
P0

Description:
Document backend APIs consumed by the Student Web App.

Goal:
Complete Create Student Web API Contract Map safely for the deferred Student Web App phase.

Expected output:
docs/phase-7/student-web-api-contract-map.md

Dependencies:
P7-003, P18-091

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-007:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-008

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-008 only.

Task:
Create Student Web Security Model

Branch:
phase7/P7-008-student-web-security-model

Priority:
P0

Description:
Document web auth, route guards, token handling, XSS/CSRF, and no direct DB/AIM rules.

Goal:
Complete Create Student Web Security Model safely for the deferred Student Web App phase.

Expected output:
docs/security/phase-7-student-web-security-model.md

Dependencies:
P7-004, P7-007

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-008:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-009

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-009 only.

Task:
Create Student Web Accessibility and RTL Plan

Branch:
phase7/P7-009-student-web-accessibility-rtl-plan

Priority:
P1

Description:
Define accessibility, Arabic/English, RTL, keyboard, and responsive requirements.

Goal:
Complete Create Student Web Accessibility and RTL Plan safely for the deferred Student Web App phase.

Expected output:
docs/phase-7/student-web-accessibility-rtl-plan.md

Dependencies:
P7-005

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-009:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-010

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-010 only.

Task:
Create Student Web App Project Shell

Branch:
phase7/P7-010-student-web-project-shell

Priority:
P0

Description:
Create or align Student Web App shell using repo conventions and design system.

Goal:
Complete Create Student Web App Project Shell safely for the deferred Student Web App phase.

Expected output:
apps/student-web/ or equivalent shell

Dependencies:
P7-001, P7-005, P7-006

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-010:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-011

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-011 only.

Task:
Create Student Web Config and Environment Setup

Branch:
phase7/P7-011-student-web-config-env

Priority:
P0

Description:
Add safe public env config, API base URL config, and no-secret rules.

Goal:
Complete Create Student Web Config and Environment Setup safely for the deferred Student Web App phase.

Expected output:
Student web config/env files and docs

Dependencies:
P7-010, P7-008

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-011:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-012

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-012 only.

Task:
Create Student Web Routing Shell

Branch:
phase7/P7-012-student-web-routing-shell

Priority:
P0

Description:
Implement route groups, layouts, protected routes, and public routes.

Goal:
Complete Create Student Web Routing Shell safely for the deferred Student Web App phase.

Expected output:
Student web route/layout files

Dependencies:
P7-006, P7-010

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-012:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-013

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-013 only.

Task:
Create Student Web Auth Guard

Branch:
phase7/P7-013-student-web-auth-guard

Priority:
P0

Description:
Implement browser auth guard and unauthorized/expired-session states.

Goal:
Complete Create Student Web Auth Guard safely for the deferred Student Web App phase.

Expected output:
Student web auth guard

Dependencies:
P7-008, P7-012

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-013:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-014

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-014 only.

Task:
Create Student Web API Client Layer

Branch:
phase7/P7-014-student-web-api-client

Priority:
P0

Description:
Implement typed backend API client with safe error handling and no direct DB access.

Goal:
Complete Create Student Web API Client Layer safely for the deferred Student Web App phase.

Expected output:
Student web API client

Dependencies:
P7-007, P7-011

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-014:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-015

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-015 only.

Task:
Create Student Web Common UI Components

Branch:
phase7/P7-015-student-web-common-ui-components

Priority:
P0

Description:
Build buttons, cards, forms, modals, banners, empty/loading/error states using AIM design system.

Goal:
Complete Create Student Web Common UI Components safely for the deferred Student Web App phase.

Expected output:
Student web common components

Dependencies:
P7-005, P7-010

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-015:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-016

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-016 only.

Task:
Create Student Web Layout System

Branch:
phase7/P7-016-student-web-layout-system

Priority:
P0

Description:
Build responsive app shell, sidebar/topbar/mobile nav, and page containers using AIM design system.

Goal:
Complete Create Student Web Layout System safely for the deferred Student Web App phase.

Expected output:
Student web layout components

Dependencies:
P7-012, P7-015

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-016:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-017

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-017 only.

Task:
Create Student Web Auth Pages

Branch:
phase7/P7-017-student-web-auth-pages

Priority:
P0

Description:
Build login/register/forgot-password/session-expired pages using AIM design system.

Goal:
Complete Create Student Web Auth Pages safely for the deferred Student Web App phase.

Expected output:
Student web auth pages

Dependencies:
P7-013, P7-014, P7-015

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-017:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-018

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-018 only.

Task:
Create Student Web Profile Page

Branch:
phase7/P7-018-student-web-profile-page

Priority:
P1

Description:
Build profile display/edit page for backend-approved fields only.

Goal:
Complete Create Student Web Profile Page safely for the deferred Student Web App phase.

Expected output:
Student web profile page

Dependencies:
P7-014, P7-016

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-018:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-019

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-019 only.

Task:
Create Student Web Settings Page

Branch:
phase7/P7-019-student-web-settings-page

Priority:
P1

Description:
Build settings for locale, account actions, and safe preferences.

Goal:
Complete Create Student Web Settings Page safely for the deferred Student Web App phase.

Expected output:
Student web settings page

Dependencies:
P7-016, P7-018

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-019:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-020

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-020 only.

Task:
Create Student Web Dashboard Home

Branch:
phase7/P7-020-student-web-dashboard-home

Priority:
P0

Description:
Build dashboard home with backend-approved progress, next lesson, recommendations, deadlines, and activity summary.

Goal:
Complete Create Student Web Dashboard Home safely for the deferred Student Web App phase.

Expected output:
Student web dashboard home

Dependencies:
P7-014, P7-016, P15/P13/P10/P6 outputs

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-020:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-021

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-021 only.

Task:
Create Student Web Dashboard Widgets

Branch:
phase7/P7-021-student-web-dashboard-widgets

Priority:
P1

Description:
Build reusable dashboard widgets for streak, progress, recommendation, deadline, and activity states.

Goal:
Complete Create Student Web Dashboard Widgets safely for the deferred Student Web App phase.

Expected output:
Student web dashboard widgets

Dependencies:
P7-020

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-021:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-022

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-022 only.

Task:
Create Student Web Progress Summary

Branch:
phase7/P7-022-student-web-progress-summary

Priority:
P0

Description:
Build backend-approved progress summary page with no local mastery calculation.

Goal:
Complete Create Student Web Progress Summary safely for the deferred Student Web App phase.

Expected output:
Student web progress summary page

Dependencies:
P7-014, P7-020, P6/P15 outputs

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-022:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-023

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-023 only.

Task:
Create Student Web Skill State Page

Branch:
phase7/P7-023-student-web-skill-state-page

Priority:
P0

Description:
Build skill-state, weakness, and recommendation view from backend-approved data only.

Goal:
Complete Create Student Web Skill State Page safely for the deferred Student Web App phase.

Expected output:
Student web skill state page

Dependencies:
P7-022, P6/P15 outputs

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-023:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-024

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-024 only.

Task:
Add Student Web Progress Authority Tests

Branch:
phase7/P7-024-student-web-progress-tests

Priority:
P0

Description:
Test dashboard/progress pages do not calculate mastery, weakness, recommendations, or AIM outputs locally.

Goal:
Complete Add Student Web Progress Authority Tests safely for the deferred Student Web App phase.

Expected output:
Student web progress tests

Dependencies:
P7-020..P7-023

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-024:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-025

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-025 only.

Task:
Create Student Web Placement Entry Page

Branch:
phase7/P7-025-student-web-placement-entry

Priority:
P0

Description:
Build placement entry/status page using backend placement APIs.

Goal:
Complete Create Student Web Placement Entry Page safely for the deferred Student Web App phase.

Expected output:
Student web placement entry UI

Dependencies:
P4 placement outputs, P7-014, P7-016

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-025:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-026

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-026 only.

Task:
Create Student Web Placement Question UI

Branch:
phase7/P7-026-student-web-placement-question-ui

Priority:
P0

Description:
Build placement question/answer UI using AIM design system.

Goal:
Complete Create Student Web Placement Question UI safely for the deferred Student Web App phase.

Expected output:
Student web placement question UI

Dependencies:
P7-025

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-026:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-027

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-027 only.

Task:
Create Student Web Placement Submit Flow

Branch:
phase7/P7-027-student-web-placement-submit-flow

Priority:
P0

Description:
Submit placement answers through backend API only.

Goal:
Complete Create Student Web Placement Submit Flow safely for the deferred Student Web App phase.

Expected output:
Student web placement submit flow

Dependencies:
P7-026

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-027:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-028

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-028 only.

Task:
Create Student Web Placement Result Page

Branch:
phase7/P7-028-student-web-placement-result-page

Priority:
P0

Description:
Display backend-approved placement result and next steps.

Goal:
Complete Create Student Web Placement Result Page safely for the deferred Student Web App phase.

Expected output:
Student web placement result page

Dependencies:
P7-027

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-028:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-029

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-029 only.

Task:
Add Student Web Placement Tests

Branch:
phase7/P7-029-student-web-placement-tests

Priority:
P0

Description:
Test placement flow, submit flow, forbidden states, and no local scoring.

Goal:
Complete Add Student Web Placement Tests safely for the deferred Student Web App phase.

Expected output:
Student web placement tests

Dependencies:
P7-025..P7-028

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-029:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-030

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-030 only.

Task:
Create Student Web Course Catalog

Branch:
phase7/P7-030-student-web-course-catalog

Priority:
P0

Description:
Build course catalog/list page with backend-approved course data.

Goal:
Complete Create Student Web Course Catalog safely for the deferred Student Web App phase.

Expected output:
Student web course catalog

Dependencies:
P3/P11 outputs, P7-014, P7-016

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-030:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-031

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-031 only.

Task:
Create Student Web Course Detail Page

Branch:
phase7/P7-031-student-web-course-detail

Priority:
P0

Description:
Build course detail with chapters, lessons, status indicators, and progress states from backend.

Goal:
Complete Create Student Web Course Detail Page safely for the deferred Student Web App phase.

Expected output:
Student web course detail page

Dependencies:
P7-030

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-031:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-032

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-032 only.

Task:
Create Student Web Chapter and Lesson List

Branch:
phase7/P7-032-student-web-chapter-lesson-list

Priority:
P0

Description:
Build chapter/lesson list UI with progress, lock, and completion states from backend.

Goal:
Complete Create Student Web Chapter and Lesson List safely for the deferred Student Web App phase.

Expected output:
Student web chapter/lesson list

Dependencies:
P7-031

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-032:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-033

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-033 only.

Task:
Create Student Web Lesson Player Shell

Branch:
phase7/P7-033-student-web-lesson-player-shell

Priority:
P0

Description:
Build responsive lesson player shell using AIM design system.

Goal:
Complete Create Student Web Lesson Player Shell safely for the deferred Student Web App phase.

Expected output:
Student web lesson player shell

Dependencies:
P7-032

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-033:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-034

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-034 only.

Task:
Create Student Web Content Block Renderer

Branch:
phase7/P7-034-student-web-content-block-renderer

Priority:
P0

Description:
Render lesson content blocks from backend safely.

Goal:
Complete Create Student Web Content Block Renderer safely for the deferred Student Web App phase.

Expected output:
Student web content block renderer

Dependencies:
P7-033, P3 content outputs

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-034:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-035

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-035 only.

Task:
Create Student Web Lesson Navigation

Branch:
phase7/P7-035-student-web-lesson-navigation

Priority:
P1

Description:
Build previous/next/complete/review navigation using backend-approved lesson state.

Goal:
Complete Create Student Web Lesson Navigation safely for the deferred Student Web App phase.

Expected output:
Student web lesson navigation

Dependencies:
P7-033, P7-034

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-035:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-036

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-036 only.

Task:
Create Lesson Progress Sync Flow

Branch:
phase7/P7-036-student-web-lesson-progress-sync

Priority:
P0

Description:
Submit lesson completion/progress events through backend APIs only.

Goal:
Complete Create Lesson Progress Sync Flow safely for the deferred Student Web App phase.

Expected output:
Student web lesson progress flow

Dependencies:
P7-035, P5/P6 outputs

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-036:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-037

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-037 only.

Task:
Add Student Web Curriculum Tests

Branch:
phase7/P7-037-student-web-curriculum-tests

Priority:
P0

Description:
Test catalog/detail/lesson/content/progress sync and no client authority.

Goal:
Complete Add Student Web Curriculum Tests safely for the deferred Student Web App phase.

Expected output:
Student web curriculum tests

Dependencies:
P7-030..P7-036

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-037:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-038

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-038 only.

Task:
Create Student Web Practice Shell

Branch:
phase7/P7-038-student-web-practice-shell

Priority:
P0

Description:
Build practice session shell for questions/attempts using AIM design system.

Goal:
Complete Create Student Web Practice Shell safely for the deferred Student Web App phase.

Expected output:
Student web practice shell

Dependencies:
P5/P6 outputs, P7-014

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-038:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-039

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-039 only.

Task:
Create Student Web Question Renderer

Branch:
phase7/P7-039-student-web-question-renderer

Priority:
P0

Description:
Render supported question types and choices safely.

Goal:
Complete Create Student Web Question Renderer safely for the deferred Student Web App phase.

Expected output:
Student web question renderer

Dependencies:
P7-038, P3 question bank outputs

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-039:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-040

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-040 only.

Task:
Create Student Web Answer Draft State

Branch:
phase7/P7-040-student-web-answer-draft-state

Priority:
P1

Description:
Implement local draft state only for unsent answers without scoring decisions.

Goal:
Complete Create Student Web Answer Draft State safely for the deferred Student Web App phase.

Expected output:
Student web answer draft state

Dependencies:
P7-039

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-040:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-041

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-041 only.

Task:
Create Student Web Practice Submit Flow

Branch:
phase7/P7-041-student-web-practice-submit-flow

Priority:
P0

Description:
Submit practice attempts to backend and display backend-approved feedback/results.

Goal:
Complete Create Student Web Practice Submit Flow safely for the deferred Student Web App phase.

Expected output:
Student web practice submit flow

Dependencies:
P7-039, P7-040

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-041:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-042

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-042 only.

Task:
Create Student Web Practice Feedback UI

Branch:
phase7/P7-042-student-web-practice-feedback-ui

Priority:
P0

Description:
Display backend-approved feedback, mistakes, and next actions.

Goal:
Complete Create Student Web Practice Feedback UI safely for the deferred Student Web App phase.

Expected output:
Student web practice feedback UI

Dependencies:
P7-041

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-042:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-043

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-043 only.

Task:
Add Student Web Practice Authority Tests

Branch:
phase7/P7-043-student-web-practice-tests

Priority:
P0

Description:
Test practice UI does not calculate correctness, score, mastery, weakness, or recommendations locally.

Goal:
Complete Add Student Web Practice Authority Tests safely for the deferred Student Web App phase.

Expected output:
Student web practice tests

Dependencies:
P7-038..P7-042

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-043:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-044

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-044 only.

Task:
Create Student Web Assessment List

Branch:
phase7/P7-044-student-web-assessment-list

Priority:
P0

Description:
Build quizzes/exams/deadlines list using backend-approved data.

Goal:
Complete Create Student Web Assessment List safely for the deferred Student Web App phase.

Expected output:
Student web assessment list

Dependencies:
P10 outputs, P7-014

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-044:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-045

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-045 only.

Task:
Create Student Web Assessment Detail Page

Branch:
phase7/P7-045-student-web-assessment-detail

Priority:
P0

Description:
Build quiz/exam detail with rules, attempts, deadline, and eligibility from backend.

Goal:
Complete Create Student Web Assessment Detail Page safely for the deferred Student Web App phase.

Expected output:
Student web assessment detail

Dependencies:
P7-044

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-045:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-046

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-046 only.

Task:
Create Student Web Start Attempt Flow

Branch:
phase7/P7-046-student-web-start-attempt-flow

Priority:
P0

Description:
Start quiz/exam attempt through backend API only.

Goal:
Complete Create Student Web Start Attempt Flow safely for the deferred Student Web App phase.

Expected output:
Student web start attempt flow

Dependencies:
P7-045, P10 outputs

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-046:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-047

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-047 only.

Task:
Create Student Web Attempt Screen

Branch:
phase7/P7-047-student-web-attempt-screen

Priority:
P0

Description:
Build web attempt-taking UI with timer/status from backend rules.

Goal:
Complete Create Student Web Attempt Screen safely for the deferred Student Web App phase.

Expected output:
Student web attempt screen

Dependencies:
P7-046

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-047:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-048

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-048 only.

Task:
Create Student Web Submit Attempt Flow

Branch:
phase7/P7-048-student-web-submit-attempt-flow

Priority:
P0

Description:
Submit quiz/exam answers to backend only and show backend-approved status.

Goal:
Complete Create Student Web Submit Attempt Flow safely for the deferred Student Web App phase.

Expected output:
Student web submit attempt flow

Dependencies:
P7-047

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-048:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-049

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-049 only.

Task:
Create Student Web Assessment Result Page

Branch:
phase7/P7-049-student-web-assessment-result

Priority:
P0

Description:
Display backend-approved scores, results, and breakdowns only.

Goal:
Complete Create Student Web Assessment Result Page safely for the deferred Student Web App phase.

Expected output:
Student web assessment result page

Dependencies:
P7-048

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-049:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-050

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-050 only.

Task:
Add Student Web Assessment Authority Tests

Branch:
phase7/P7-050-student-web-assessment-tests

Priority:
P0

Description:
Test no local scoring, correctness, deadline authority, pass/fail, mastery, or result calculation.

Goal:
Complete Add Student Web Assessment Authority Tests safely for the deferred Student Web App phase.

Expected output:
Student web assessment tests

Dependencies:
P7-044..P7-049

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-050:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-051

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-051 only.

Task:
Create Student Web AI Teacher Entry

Branch:
phase7/P7-051-student-web-ai-teacher-entry

Priority:
P1

Description:
Build AI Teacher entry point and conversation launcher using AIM design system.

Goal:
Complete Create Student Web AI Teacher Entry safely for the deferred Student Web App phase.

Expected output:
Student web AI Teacher entry

Dependencies:
P18 outputs, P7-014

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-051:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-052

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-052 only.

Task:
Create Student Web AI Chat UI

Branch:
phase7/P7-052-student-web-ai-chat-ui

Priority:
P1

Description:
Build web AI chat UI using backend AI Teacher APIs and design system.

Goal:
Complete Create Student Web AI Chat UI safely for the deferred Student Web App phase.

Expected output:
Student web AI chat UI

Dependencies:
P7-051, P18 outputs

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-052:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-053

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-053 only.

Task:
Create Student Web AI History and Feedback UI

Branch:
phase7/P7-053-student-web-ai-history-feedback

Priority:
P2

Description:
Build AI history and feedback surfaces using backend-approved data.

Goal:
Complete Create Student Web AI History and Feedback UI safely for the deferred Student Web App phase.

Expected output:
Student web AI history/feedback UI

Dependencies:
P7-052

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-053:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-054

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-054 only.

Task:
Add Student Web AI Teacher Tests

Branch:
phase7/P7-054-student-web-ai-tests

Priority:
P1

Description:
Test chat/history/feedback/safety states and no progress/AIM mutation authority.

Goal:
Complete Add Student Web AI Teacher Tests safely for the deferred Student Web App phase.

Expected output:
Student web AI tests

Dependencies:
P7-051..P7-053

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-054:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-055

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-055 only.

Task:
Create Student Web Notification Center

Branch:
phase7/P7-055-student-web-notification-center

Priority:
P1

Description:
Build notification inbox/read/dismiss UI using backend APIs and design system.

Goal:
Complete Create Student Web Notification Center safely for the deferred Student Web App phase.

Expected output:
Student web notification center

Dependencies:
P13 outputs, P7-014

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-055:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-056

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-056 only.

Task:
Create Student Web Notification Preferences

Branch:
phase7/P7-056-student-web-notification-preferences

Priority:
P2

Description:
Build notification/reminder preferences UI using backend APIs only.

Goal:
Complete Create Student Web Notification Preferences safely for the deferred Student Web App phase.

Expected output:
Student web notification preferences

Dependencies:
P7-055

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-056:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-057

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-057 only.

Task:
Add Student Web Notification Tests

Branch:
phase7/P7-057-student-web-notification-tests

Priority:
P2

Description:
Test inbox/preferences/read/dismiss/error states and no schedule/delivery authority.

Goal:
Complete Add Student Web Notification Tests safely for the deferred Student Web App phase.

Expected output:
Student web notification tests

Dependencies:
P7-055, P7-056

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-057:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-058

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-058 only.

Task:
Create Student Web Billing Pages

Branch:
phase7/P7-058-student-web-billing-pages

Priority:
P2

Description:
Build pricing/subscription/invoice pages using backend billing APIs and design system.

Goal:
Complete Create Student Web Billing Pages safely for the deferred Student Web App phase.

Expected output:
Student web billing pages

Dependencies:
P14 outputs, P7-014

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-058:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-059

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-059 only.

Task:
Create Student Web Checkout Flow

Branch:
phase7/P7-059-student-web-checkout-flow

Priority:
P2

Description:
Start checkout through backend API and display backend-approved status.

Goal:
Complete Create Student Web Checkout Flow safely for the deferred Student Web App phase.

Expected output:
Student web checkout flow

Dependencies:
P7-058, P14 outputs

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-059:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-060

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-060 only.

Task:
Add Student Web Billing Tests

Branch:
phase7/P7-060-student-web-billing-tests

Priority:
P2

Description:
Test pricing/checkout/subscription/invoice states and no payment/entitlement authority.

Goal:
Complete Add Student Web Billing Tests safely for the deferred Student Web App phase.

Expected output:
Student web billing tests

Dependencies:
P7-058, P7-059

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-060:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-061

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-061 only.

Task:
Create Student Web Reports Summary

Branch:
phase7/P7-061-student-web-reports-summary

Priority:
P2

Description:
Build web student analytics/report summary using backend-approved metrics.

Goal:
Complete Create Student Web Reports Summary safely for the deferred Student Web App phase.

Expected output:
Student web reports summary

Dependencies:
P15 outputs, P7-014

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-061:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-062

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-062 only.

Task:
Add Student Web Reports Authority Tests

Branch:
phase7/P7-062-student-web-reports-tests

Priority:
P2

Description:
Test report UI does not calculate official metrics, progress, AIM, assessment, billing, or notification outcomes.

Goal:
Complete Add Student Web Reports Authority Tests safely for the deferred Student Web App phase.

Expected output:
Student web reports tests

Dependencies:
P7-061

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-062:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-063

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-063 only.

Task:
Create Student Web Support and Feedback UI

Branch:
phase7/P7-063-student-web-support-feedback

Priority:
P2

Description:
Build help, support ticket, feedback, release notes, and status pages using design system.

Goal:
Complete Create Student Web Support and Feedback UI safely for the deferred Student Web App phase.

Expected output:
Student web support/feedback UI

Dependencies:
P17 outputs, P7-014

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-063:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-064

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-064 only.

Task:
Add Student Web Support Tests

Branch:
phase7/P7-064-student-web-support-tests

Priority:
P2

Description:
Test support/feedback/status/release notes and ownership/privacy states.

Goal:
Complete Add Student Web Support Tests safely for the deferred Student Web App phase.

Expected output:
Student web support tests

Dependencies:
P7-063

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-064:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-065

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-065 only.

Task:
Apply Student Web Responsive Pass

Branch:
phase7/P7-065-student-web-responsive-pass

Priority:
P1

Description:
Review and fix responsive behavior across all web student pages using AIM design system.

Goal:
Complete Apply Student Web Responsive Pass safely for the deferred Student Web App phase.

Expected output:
Student web responsive updates

Dependencies:
P7-017..P7-064

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-065:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-066

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-066 only.

Task:
Apply Student Web RTL and Localization Pass

Branch:
phase7/P7-066-student-web-rtl-localization-pass

Priority:
P1

Description:
Review Arabic/English, RTL layout, date/number formatting, and text direction across student web UI.

Goal:
Complete Apply Student Web RTL and Localization Pass safely for the deferred Student Web App phase.

Expected output:
Student web RTL/localization updates

Dependencies:
P7-017..P7-064

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-066:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-067

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-067 only.

Task:
Apply Student Web Accessibility Pass

Branch:
phase7/P7-067-student-web-accessibility-pass

Priority:
P1

Description:
Review keyboard navigation, focus states, labels, forms, tables, dialogs, and screen-reader structure.

Goal:
Complete Apply Student Web Accessibility Pass safely for the deferred Student Web App phase.

Expected output:
Student web accessibility updates

Dependencies:
P7-017..P7-064

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-067:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-068

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-068 only.

Task:
Create Student Web Design System Review

Branch:
phase7/P7-068-student-web-design-system-review

Priority:
P0

Description:
Verify all student web UI follows AIM design system tokens/components and no one-off styling.

Goal:
Complete Create Student Web Design System Review safely for the deferred Student Web App phase.

Expected output:
docs/quality/phase-7-student-web-design-system-review.md

Dependencies:
P7-065, P7-066, P7-067

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-068:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-069

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-069 only.

Task:
Create Student Web Security Review

Branch:
phase7/P7-069-student-web-security-review

Priority:
P0

Description:
Review route guards, token handling, API access, no direct DB/AIM calls, XSS/CSRF, and sensitive data exposure.

Goal:
Complete Create Student Web Security Review safely for the deferred Student Web App phase.

Expected output:
docs/security/phase-7-student-web-security-review.md

Dependencies:
P7-008, P7-013, P7-014

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-069:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-070

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-070 only.

Task:
Create Student Web No-Client-Authority Review

Branch:
phase7/P7-070-student-web-no-client-authority-review

Priority:
P0

Description:
Prove web UI does not calculate or mutate official learning, assessment, billing, notification, analytics, or AIM authority.

Goal:
Complete Create Student Web No-Client-Authority Review safely for the deferred Student Web App phase.

Expected output:
docs/quality/phase-7-student-web-no-client-authority-review.md

Dependencies:
P7-024, P7-043, P7-050, P7-054, P7-060, P7-062

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-070:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-071

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-071 only.

Task:
Create Student Web Performance Review

Branch:
phase7/P7-071-student-web-performance-review

Priority:
P1

Description:
Review bundle size, page load, API loading, lesson rendering, assessment UI, dashboard widgets, and chat performance.

Goal:
Complete Create Student Web Performance Review safely for the deferred Student Web App phase.

Expected output:
docs/performance/phase-7-student-web-performance-review.md

Dependencies:
P7-017..P7-064

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-071:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-072

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-072 only.

Task:
Create Student Web Auth and Dashboard E2E Check

Branch:
phase7/P7-072-student-web-e2e-auth-dashboard

Priority:
P1

Description:
Document or implement E2E check for login, dashboard, progress, and settings.

Goal:
Complete Create Student Web Auth and Dashboard E2E Check safely for the deferred Student Web App phase.

Expected output:
docs/quality/phase-7-student-web-auth-dashboard-e2e-check.md

Dependencies:
P7-017..P7-024

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-072:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-073

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-073 only.

Task:
Create Student Web Learning E2E Check

Branch:
phase7/P7-073-student-web-e2e-learning

Priority:
P1

Description:
Document or implement E2E check for course, lesson, practice, and progress sync.

Goal:
Complete Create Student Web Learning E2E Check safely for the deferred Student Web App phase.

Expected output:
docs/quality/phase-7-student-web-learning-e2e-check.md

Dependencies:
P7-030..P7-043

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-073:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-074

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-074 only.

Task:
Create Student Web Assessment E2E Check

Branch:
phase7/P7-074-student-web-e2e-assessment

Priority:
P1

Description:
Document or implement E2E check for assessment list, attempt, submit, and result.

Goal:
Complete Create Student Web Assessment E2E Check safely for the deferred Student Web App phase.

Expected output:
docs/quality/phase-7-student-web-assessment-e2e-check.md

Dependencies:
P7-044..P7-050

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-074:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-075

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-075 only.

Task:
Create Student Web AI/Notifications/Billing E2E Check

Branch:
phase7/P7-075-student-web-e2e-ai-notifications-billing

Priority:
P2

Description:
Document or implement E2E check for AI chat, notifications, and billing surfaces.

Goal:
Complete Create Student Web AI/Notifications/Billing E2E Check safely for the deferred Student Web App phase.

Expected output:
docs/quality/phase-7-student-web-ai-notifications-billing-e2e-check.md

Dependencies:
P7-051..P7-060

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-075:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


UI Design System Requirements:
- Follow the approved AIM design system from docs/design/source/aim-design-system.
- Use existing tokens, spacing, typography, colors, radius, elevation, layout rules, and component contracts.
- Use shared student-web/common components where available.
- Do not introduce one-off styling, random colors, custom spacing, or inconsistent UI primitives.
- Support responsive layout, Arabic/RTL readiness, accessibility, and safe loading/empty/error/forbidden states.

Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-076

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-076 only.

Task:
Create Student Web Architecture Review

Branch:
phase7/P7-076-student-web-architecture-review

Priority:
P0

Description:
Review web architecture, feature boundaries, API use, design system use, authority boundaries, and maintainability.

Goal:
Complete Create Student Web Architecture Review safely for the deferred Student Web App phase.

Expected output:
docs/quality/phase-7-student-web-architecture-review.md

Dependencies:
P7-068, P7-069, P7-070, P7-071

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-076:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-077

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-077 only.

Task:
Create Phase 7 Output Completeness Review

Branch:
phase7/P7-077-output-completeness-review

Priority:
P0

Description:
Verify every Phase 7 expected output exists and respects deferred execution, design, security, and authority rules.

Goal:
Complete Create Phase 7 Output Completeness Review safely for the deferred Student Web App phase.

Expected output:
docs/quality/phase-7-output-completeness-review.md

Dependencies:
P7-072..P7-076

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-077:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.


#P7-078

You are working on AIM Platform Phase 7 — Deferred Student Web App.

Work on task P7-078 only.

Task:
Create Phase 7 Final Review and Handoff

Branch:
phase7/P7-078-phase-7-final-review

Priority:
P0

Description:
Summarize Student Web App implementation, outputs, risks, checks, limitations, and next steps.

Goal:
Complete Create Phase 7 Final Review and Handoff safely for the deferred Student Web App phase.

Expected output:
docs/phase-7/final-review.md

Dependencies:
P7-077

Mandatory deferred execution rule:
- This Phase 7 is intentionally delayed.
- Do not start this task until all Phase 1..18 final reviews are complete.
- Confirm P18-091 and docs/phase-18/final-review.md exist before editing.

Required workflow:
1. Open the Phase 7 Notion database.
2. Confirm Phase 1..18 final reviews are complete.
3. Pick this task only if Status = Undone and Assigned is empty.
4. Verify dependencies are Done and outputs exist in GitHub.
5. Assign the task to yourself before editing.
6. Set Status = In Progress before editing.
7. Re-open the task and confirm assignment/status.
8. Create/use the exact branch listed above.
9. Implement only the expected output for this task.
10. Commit every changed file with a message starting with "P7-078:".
11. Push the branch.
12. Add the completion comment.
13. Set Status = Done only after push succeeds.


Authority and safety rules:
- Student Web App is a browser client only.
- It must consume backend APIs only.
- It must not call the database directly.
- It must not call AIM Engine directly.
- It must not calculate or mutate official mastery, weakness, difficulty, recommendations, review schedules, progress, placement results, assessment scores, billing state, notification delivery state, analytics outputs, or AIM outputs.
- Backend/AIM remains final authority for learning decisions.
- Do not commit secrets, service-role keys, provider keys, database credentials, signing keys, or production tokens.

Done test:
- Expected output exists.
- Deferred-after-Phase-18 rule is respected.
- UI tasks follow AIM design system when applicable.
- No client-side authority is introduced.
- No direct DB/AIM access is introduced.
- No unrelated phase work is introduced.
- No secrets are present.
