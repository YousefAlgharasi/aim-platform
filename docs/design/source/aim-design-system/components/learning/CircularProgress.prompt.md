Circular progress dial for daily goals and quiz scores.

```jsx
<CircularProgress value={68} tone="gradient" caption="daily goal" />
<CircularProgress value={9} max={10} label={<span style={{font:'var(--type-h2)'}}>9/10</span>} caption="score" tone="success" />
```
