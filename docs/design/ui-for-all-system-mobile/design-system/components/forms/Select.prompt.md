Dropdown select styled to match AIM inputs. Pass `options` as strings or `{value,label}` objects.

```jsx
<Select label="Level" placeholder="Choose a level" options={['Beginner','Intermediate','Advanced']} />
```
The chevron and padding are RTL-aware.
