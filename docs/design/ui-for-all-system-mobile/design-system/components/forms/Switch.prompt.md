Toggle switch for instant on/off settings (notifications, dark mode, reminders). RTL-aware — the thumb travels the correct direction.

```jsx
<Switch label="Daily reminders" checked={on} onChange={e=>setOn(e.target.checked)} />
```
