# 10 — Known Problems

> Last verified: 2026-07-04. Sourced from `project-memory.json.known_issues` (kept in sync) plus new findings from this session's full inventory pass. Each entry: what's wrong, where, how verified, current status.

| ID | What's wrong | Where | How verified | Status |
|---|---|---|---|---|
| `rls-disabled-eight-tables` | 8 tables had RLS disabled, fully exposed to anon/authenticated Supabase roles. | `student_learning_goals`, `daily_challenge_templates`, `admin_broadcast_schedules`, `achievement_definitions`, `student_achievements`, `xp_levels`, `student_level_state`, `ai_focus_directives` | Live Supabase advisor + `pg_tables` query | **Fixed** 2026-07-04, migration `20260704030000_apply_rls_missing_tables` |
| `backend-env-committed-secrets` | `services/backend-api/backend.env` was tracked in git with a real service-role key, DB URL, JWT secret, AI/STT keys. | `services/backend-api/backend.env` | Direct file read | **Partially fixed** — untracked + `.gitignore` hardened; **keys still in git history, not rotated** |
| `migration-ledger-out-of-band-gaps` | 11 migrations applied live via `mcp__Supabase__apply_migration` were missing from `_prisma_migrations`. | `services/backend-api/prisma/migrations/*` vs. live `_prisma_migrations` | Direct diff of repo migration dirs vs. live table rows | **Fixed** 2026-07-04, real sha256 checksums inserted |
| `services-api-not-actually-reference-only` | `PROJECT_STATE.md`/`project-memory.json` call `services/api` reference-only; it is actually live-imported by `services/aim-engine` at runtime (6 domain services via `sys.path` hack). | `services/aim-engine/app/pipeline/aim_analysis_pipeline.py:48-50,303-309` | Direct code read + repo-wide grep for every `services/api`-referencing file, classified comment-vs-real-import | **Open** — a documentation/architecture-boundary discrepancy, not itself a bug |
| `voice-message-submit-module-unwired` | `VoiceMessageSubmitModule` + 4 other services (`AiTeacherFallbackService`, `AnswerSubmissionService`, `QuestionDeliveryService` in assessments, `ParentAccessAuditService`, `AudioUploadSafeFailureService`) are built but never imported by any module. | `services/backend-api/src/features/voice-teacher/message-submit/`, `ai-teacher/ai-teacher-fallback.service.ts`, `assessments/answer-submission.service.ts` + `question-delivery.service.ts`, `parents/parent-access-audit.service.ts`, `voice-teacher/audio-upload/audio-upload-safe-failure.service.ts` | Exhaustive grep of every class name against every `*.module.ts` provider list | **Open** — `VoiceMessageSubmitModule` is self-documented as intentionally deferred (P9-068+); the other 4 are not documented as intentional |
| `notification-delivery-worker-never-invoked` | `NotificationDeliveryWorker.processQueue()` is never called by any cron or controller in production code; notifications get enqueued but never dispatched. Compounded by both push and email adapters being no-op stubs with no real SDK. | `features/notifications/notification-delivery.worker.ts`, `notifications.module.ts` (Noop adapter bindings) | Grep for `processQueue` call sites (only found in the worker itself and its unit test); grep for FCM/APNs/SendGrid/nodemailer imports (zero found) | **Open** |
| `payment-provider-adapter-stubbed-empty` | `PAYMENT_PROVIDER_ADAPTER` is bound to a literal empty object `{}` in `billing.module.ts`. Interface is Stripe-shaped; no real Stripe SDK or any payment SDK import exists outside test mocks. | `services/backend-api/src/features/billing/billing.module.ts` (~line 46), `payment-provider.adapter.ts` | Direct code read + repo-wide grep for `stripe` package import | **Open** |
| `student-web-api-prefix-mismatch-reopened` | `notifications`/`parents` controllers hardcode `api/v1/...`; every other controller is bare (no global prefix set anywhere). No single `REACT_APP_API_BASE_URL` value makes both groups resolve correctly simultaneously. | `services/backend-api/src/features/notifications/*.controller.ts`, `parents/*.controller.ts`, `main.ts` (no `setGlobalPrefix`), `apps/student-web/src/config/env.ts` + `.env.example` | Direct code read of controller decorators and frontend base-URL construction | **Open** — whether it's actually broken depends on the real deployed env value, which is **Unknown** |
| `dependabot-config-malformed-filename` | `.github/dependabotggggggg.yml` typo'd filename means GitHub never reads it; still references a stale `/apps/web` entry. | `.github/dependabotggggggg.yml` | Directory listing | **Open** |
| `apps-web-no-ci-coverage` | `apps/web` has no CI workflow at all, unlike `admin-dashboard`/`student-web`. | `.github/workflows/*` (grepped for `apps/web`, zero matches) | Direct grep across every workflow file | **Open** |
| `audio-cleanup-service-never-scheduled` | `AudioCleanupService`/`AudioCleanupModule` is wired as a module but no scheduler invokes `runCleanup()`; the module's own header says cleanup is meant to run "on a configurable interval" via a job that doesn't exist yet. | `features/voice-teacher/audio-cleanup/audio-cleanup.module.ts`, `audio-cleanup.service.ts:17` | Grep for every `@Cron`/scheduler reference repo-wide — only `NotificationReminderScheduler`'s 4 jobs found | **Open** |
| `assessment-attempt-expiry-job-missing` | `assessment-attempt.service.ts:166` references an expected "backend-scheduled job" for attempt expiry that does not exist anywhere in the codebase. | `features/assessments/assessment-attempt.service.ts:166` | Grep for any scheduler tied to attempt expiry — none found beyond the notifications cron | **Open** |
| `voice-audio-assets-fk-decoupled` | `voice_audio_assets.message_id` (FK to legacy `voice_messages`) made nullable; new rows use `ai_chat_message_id` instead. | migration `20260704020000_decouple_voice_audio_assets_from_voice_messages` | Prior session's PR #1305 | **Fixed**, PR #1305 (P21-021b) |
| `audio-upload-service-stale-session-check` | `AudioUploadService` checked ownership against `voice_sessions`, which stopped receiving new rows as of P21-007. | `features/voice-teacher/audio-upload/audio-upload.service.ts` | Prior session's PR #1305 | **Fixed**, PR #1305 (P21-021b) — was latent, not an active incident, since the affected module isn't wired |

## Newly found this session, not yet in a prior audit

The 4 orphaned-service findings, the notification-delivery gap, the
payment-provider stub, the audio-cleanup scheduling gap, and the
assessment-attempt-expiry gap were all found during this session's full
inventory pass and are listed above (also added to `project-memory.json.known_issues`).
