# Phase 3 - Skill Contract

This file is the prompt output alias for P3-011.

The canonical shared contract for this task is:

```text
packages/shared-contracts/api/skill-objective-contracts.md
```

It defines skill keys, skill fields, skill domains, objective fields, lesson-skill mappings, question-skill mappings, safe fields, and backend authority rules.
