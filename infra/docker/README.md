# infra/docker — Docker Compose Foundation

Docker Compose foundation for local development.

**Phase 1 scope:** Backend API service, AIM Engine service, and supporting services only.

**Constraints:**
- Does not include Student Web App service.
- Uses environment variable placeholders only. No real secrets.

See `docs/phase-1/repo-structure.md`.
