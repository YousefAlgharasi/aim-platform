# Backend testing foundation

This folder contains the Phase 1 backend testing foundation.

Current scope:

- Jest + ts-jest configuration.
- Test-only environment placeholders.
- Basic `GET /health` foundation test.

Rules:

- Do not commit real secrets in test files.
- Do not test full feature behavior during Phase 1 foundation tasks.
- Keep tests suitable for CI execution.
