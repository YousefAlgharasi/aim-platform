// P10-049: assessment Riverpod provider barrel.
// Providers are added in subsequent tasks (P10-053+).
// Providers manage display state only; they must not compute scores,
// correctness, pass/fail, deadline status, or attempt eligibility.
