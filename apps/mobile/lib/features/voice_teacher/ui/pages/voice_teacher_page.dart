import 'package:flutter/material.dart';

class VoiceTeacherPage extends StatelessWidget {
  const VoiceTeacherPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(
        child: Text('Voice Teacher — coming soon'),
      ),
    );
  }
}
