enum TicketCategory { technical, billing, account, content, feedback, other }

enum TicketSeverity { low, medium, high, critical }

enum TicketStatus { open, inProgress, waitingOnUser, resolved, closed }

enum FeedbackCategory { general, feature, bug, content, ux }

enum ComponentStatus { operational, degraded, partialOutage, majorOutage, maintenance }

enum MaintenanceStatus { scheduled, inProgress, completed, cancelled }
