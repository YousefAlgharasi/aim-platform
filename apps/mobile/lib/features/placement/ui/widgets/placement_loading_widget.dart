// Phase 4 — P4-061
// Placement loading widget stub.
//
// Scope: Placement Test phase only.
// Full widget implementation added in P4-066.

import 'package:flutter/material.dart';

class PlacementLoadingWidget extends StatelessWidget {
  const PlacementLoadingWidget({super.key});

  @override
  Widget build(BuildContext context) {
    // Placeholder — implemented in P4-066
    return const Center(child: CircularProgressIndicator());
  }
}
