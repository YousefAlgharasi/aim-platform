// Phase 4 — P4-061
// Placement intro page stub.
//
// Scope: Placement Test phase only.
// Full UI implementation added in P4-066.
// This page shows the placement test introduction to the student.
// Flutter must not display or compute placement score, CEFR threshold
// breakdowns, skill mastery, weakness map, or initial path.

import 'package:flutter/material.dart';

class PlacementIntroPage extends StatelessWidget {
  const PlacementIntroPage({super.key});

  @override
  Widget build(BuildContext context) {
    // Placeholder — UI implemented in P4-066
    return const Scaffold(
      body: Center(child: Text('Placement Intro — coming soon')),
    );
  }
}
